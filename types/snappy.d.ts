/**
 * Compress snappy data.
 * Writes Snappy-compressed bytes into a writer.
 *
 * @import {Writer} from '../src/types.js'
 * @param {Writer} writer
 * @param {Uint8Array} input - uncompressed data
 */
export function snappyCompress(writer: Writer, input: Uint8Array): void;
import type { Writer } from '../src/types.js';
//# sourceMappingURL=snappy.d.ts.map